services:
  - type: web
    name: hrms-backend
    env: python
    region: singapore
    plan: free
    buildCommand: |
      cd backend
      pip install --upgrade pip
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: |
      cd backend
      gunicorn hrms.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hrms-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: hrms-backend.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        value: https://hrms-frontend.onrender.com
    healthCheckPath: /api/health/
    autoDeploy: true

  - type: web
    name: hrms-frontend
    env: static
    region: singapore
    plan: free
    buildCommand: |
      cd frontend
      npm install
      npm run build
    staticPublishPath: ./frontend/dist  # Vite uses 'dist' folder, not 'build'
    envVars:
      - key: REACT_APP_API_URL
        value: https://hrms-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    autoDeploy: true

databases:
  - name: hrms-db
    plan: free
    databaseName: hrms
    user: hrms_user
    region: singapore
